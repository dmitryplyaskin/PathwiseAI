#!/usr/bin/env sh
. "$(dirname -- "$0")/_/husky.sh"

echo "🔍 Проверка измененных файлов..."

# Запускаем lint-staged для проверки измененных файлов
npx lint-staged

echo "🏗️ Проверка сборки фронтенда..."
cd frontend
yarn build > nul 2>&1
if [ $? -ne 0 ]; then
  echo "❌ Ошибка сборки фронтенда!"
  exit 1
fi
cd ..

echo "🏗️ Проверка сборки бекенда..."
yarn build > nul 2>&1
if [ $? -ne 0 ]; then
  echo "❌ Ошибка сборки бекенда!"
  exit 1
fi

echo "✅ Все проверки пройдены успешно!"