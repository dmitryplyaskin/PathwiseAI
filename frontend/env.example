## Frontend (Vite)
#
# Скопируйте этот файл в `frontend/.env` и при необходимости измените значения.
#
# Базовый URL API (обычно backend /api).
# В dev по умолчанию: http://localhost:3000/api
VITE_API_BASE_URL=http://localhost:3000/api

